VITE_API_BASE_URL=
VITE_APP_ENV=development
# Dev server proxy target for /api (Vite). Defaults to http://localhost:8000.
VITE_DEV_PROXY_TARGET=http://localhost:8000
# Comma-separated Host header allow-list for the Vite dev server. "*" or "all" allows any host.
VITE_ALLOWED_HOSTS=localhost
